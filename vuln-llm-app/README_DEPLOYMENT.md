# Deployment Notes

- Ensure OpenWebUI reachable and models imported
- Configure server with OpenWebUI base `/api` and API key (env vars)
- Register blueprint and templates; verify routes
- Validate models; badge catalog accordingly
- Monitor logs; adjust rate limits based on usage